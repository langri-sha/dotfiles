name: Workspace

on:
  pull_request:
  push:
    branches:
      - 'main'

jobs:
  codespace:
    name: Codespace
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
